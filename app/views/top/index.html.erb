<h1>Top#index</h1>
<p>
<table border="2">
<% @files.each do |file|%>
<tr>
  <td><%= link_to file,"#{root_url}/public/resources/#{file}"%></td>
  <td><%= link_to "add tag", new_tagged_file_path, :file_name => file%></td>
</tr>
<%end%>
</p>
